name: Build and Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: macos-15
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Select Xcode 16
        run: |
          XCODE_PATH="$(ls -d /Applications/Xcode_16.[0-9]*.app 2>/dev/null | grep -vi beta | grep -vi candidate | sort -V | tail -1)"
          if [ -z "$XCODE_PATH" ]; then
            echo "No stable Xcode 16.x found on runner"
            ls /Applications/Xcode*.app
            exit 1
          fi
          sudo xcode-select -s "$XCODE_PATH/Contents/Developer"

      - name: Install Apple Developer Certificate
        uses: apple-actions/import-codesign-certs@v2
        with:
          p12-file-base64: ${{ secrets.APPLE_DEVELOPER_CERTIFICATE_P12_BASE64 }}
          p12-password: ${{ secrets.APPLE_DEVELOPER_CERTIFICATE_PASSWORD }}
          keychain: build
          keychain-password: ${{ secrets.KEYCHAIN_PASSWORD }}

      - name: Resolve SPM dependencies
        run: xcodebuild -resolvePackageDependencies -project PRNotifier/PRNotifier.xcodeproj -scheme PRNotifier

      - name: Build Release
        run: |
          xcodebuild \
            -project PRNotifier/PRNotifier.xcodeproj \
            -scheme PRNotifier \
            -configuration Release \
            -derivedDataPath build/DerivedData \
            CODE_SIGN_IDENTITY="Developer ID Application" \
            DEVELOPMENT_TEAM="${{ secrets.APPLE_TEAM_ID }}" \
            clean build

      - name: Notarize app
        env:
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
          APPLE_APP_SPECIFIC_PASSWORD: ${{ secrets.APPLE_APP_SPECIFIC_PASSWORD }}
        run: |
          APP_PATH="build/DerivedData/Build/Products/Release/PRNotifier.app"

          # Create zip for notarization submission
          ditto -c -k --keepParent "$APP_PATH" build/PRNotifier-notarize.zip

          # Submit for notarization and verify acceptance
          xcrun notarytool submit build/PRNotifier-notarize.zip \
            --apple-id "$APPLE_ID" \
            --team-id "$APPLE_TEAM_ID" \
            --password "$APPLE_APP_SPECIFIC_PASSWORD" \
            --wait 2>&1 | tee notarize-output.txt

          if ! grep -iq "status: Accepted" notarize-output.txt; then
            echo "Notarization failed"
            cat notarize-output.txt
            exit 1
          fi

          # Staple the notarization ticket
          xcrun stapler staple "$APP_PATH"

          rm -f notarize-output.txt

      - name: Create DMG
        run: |
          VERSION=$(grep 'MARKETING_VERSION' PRNotifier/project.yml | head -1 | sed 's/.*: *["'"'"']*\([0-9][0-9.]*\)["'"'"']*/\1/')
          if [ -z "$VERSION" ]; then
            echo "Failed to extract MARKETING_VERSION from project.yml"
            exit 1
          fi
          APP_PATH="build/DerivedData/Build/Products/Release/PRNotifier.app"
          DMG_PATH="build/PRNotifier-${VERSION}.dmg"

          # Create staging area
          mkdir -p build/dmg-staging
          cp -a "$APP_PATH" build/dmg-staging/
          ln -s /Applications build/dmg-staging/Applications

          # Create compressed DMG
          hdiutil create \
            -srcfolder build/dmg-staging \
            -volname "PRNotifier" \
            -fs HFS+ \
            -format UDZO \
            -imagekey zlib-level=9 \
            "$DMG_PATH"

          rm -rf build/dmg-staging

          echo "DMG_PATH=$DMG_PATH" >> "$GITHUB_ENV"
          echo "VERSION=$VERSION" >> "$GITHUB_ENV"

      - name: Upload to GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: build/PRNotifier-*.dmg
          generate_release_notes: true
